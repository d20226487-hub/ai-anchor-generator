import Link from "next/link";
import { Card, CardBody, CardHeader, CardTitle } from "@/components/ui/Card";

// The docs are intentionally Russian-only for now (per project decision).
// They are written for SEO professionals who use the tool day-to-day.

export const metadata = {
  title: "Документация — AI Anchor Generator",
};

const TOC = [
  { id: "intro", label: "1. Что это и для чего" },
  { id: "quickstart", label: "2. Быстрый старт" },
  { id: "settings", label: "3. Настройки" },
  { id: "new-job", label: "4. Создание задачи" },
  { id: "modes", label: "5. Режимы: один сайт vs несколько" },
  { id: "distribution", label: "6. Распределение и категории анкоров" },
  { id: "ratios", label: "7. Соотношение dofollow / nofollow" },
  { id: "csv", label: "8. Загрузка CSV" },
  { id: "running", label: "9. Запуск, батчи и прогресс" },
  { id: "results", label: "10. Работа с результатами" },
  { id: "filters", label: "11. Фильтры и пагинация" },
  { id: "compare", label: "12. Цель vs факт" },
  { id: "rebalance", label: "13. Выравнивание распределения анкоров" },
  { id: "quickfix", label: "14. Подгонка соотношения dofollow" },
  { id: "edit-rerun", label: "15. Редактирование и перезапуск задачи" },
  { id: "export", label: "16. Экспорт результатов" },
  { id: "troubleshooting", label: "17. Решение проблем" },
];

export default function DocsPage() {
  return (
    <div className="grid grid-cols-1 lg:grid-cols-[240px_1fr] gap-8">
      <aside className="lg:sticky lg:top-16 lg:self-start lg:h-[calc(100vh-5rem)] lg:overflow-y-auto rounded-md border border-[var(--color-border)] bg-[var(--color-surface)]">
        <div className="sticky top-0 z-10 px-5 py-3 border-b border-[var(--color-border)] bg-[var(--color-surface)] font-semibold text-sm">
          Содержание
        </div>
        <nav className="p-2 space-y-0.5 text-sm">
          {TOC.map((s) => (
            <a
              key={s.id}
              href={`#${s.id}`}
              className="block px-2 py-1 rounded text-[var(--color-text-dim)] hover:text-[var(--color-text)] hover:bg-[var(--color-surface-2)]/60"
            >
              {s.label}
            </a>
          ))}
        </nav>
      </aside>

      <article className="docs-article max-w-none space-y-10">
        <header>
          <h1 className="text-3xl font-bold mb-2">Документация</h1>
          <p className="text-[var(--color-text-dim)]">
            Полное руководство по AI Anchor Generator: от настройки API-ключей до выравнивания
            распределения анкоров на тысячах URL.
          </p>
        </header>

        <Section id="intro" title="1. Что это и для чего">
          <p>
            <b>AI Anchor Generator</b> — инструмент для линкбилдеров, который генерирует естественные
            анкоры для размещаемых ссылок с заданным распределением анкоров и соотношением{" "}
            <code>dofollow / nofollow</code>. Работает с тремя AI-провайдерами:{" "}
            <b>OpenRouter</b>, <b>GitHub Models</b> и <b>Google Gemini</b>.
          </p>
          <p>Что инструмент умеет:</p>
          <ul>
            <li>Генерировать анкоры пакетами (по 10 URL в батче), что позволяет работать с тысячами URL без сбоев.</li>
            <li>Строго распределять анкоры по 4 категориям: общие, брендовые, ключевые, URL.</li>
            <li>Поддерживать заданный процент <code>dofollow</code> к <code>nofollow</code>.</li>
            <li>Работать в двух режимах: один сайт или несколько брендов одновременно.</li>
            <li>Корректировать распределение анкоров после генерации без полной перегенерации (точечно или с заменой).</li>
            <li>Сохранять задачи и возвращаться к ним: редактировать критерии, перезапускать, ставить на паузу.</li>
          </ul>
        </Section>

        <Section id="quickstart" title="2. Быстрый старт">
          <ol>
            <li>
              Откройте <Doclink href="/settings">Настройки</Doclink> → вкладка <b>Провайдеры</b>. Введите API-ключ
              хотя бы одного провайдера и нажмите <b>Проверить подключение</b>. Сохраните настройки.
            </li>
            <li>
              Перейдите в <Doclink href="/jobs/new">Новая задача</Doclink>.
            </li>
            <li>
              Задайте имя, режим (<b>Один сайт</b> для одного домена или <b>Несколько сайтов</b> для нескольких брендов).
            </li>
            <li>
              Настройте распределение анкоров по 4 категориям так, чтобы сумма = 100%.
            </li>
            <li>
              Включите соотношение <code>dofollow / nofollow</code> и выберите процент.
            </li>
            <li>
              Загрузите CSV или вставьте его в текстовое поле. Обязательная колонка — <code>Target URL</code>.
            </li>
            <li>
              Нажмите <b>Создать и сгенерировать</b>. Откроется страница задачи с прогресс-баром.
            </li>
            <li>
              После завершения проверьте таблицу <b>Цель vs факт</b>; при расхождениях используйте <b>Выровнять</b> или <b>Подогнать соотношение</b>.
            </li>
            <li>
              Экспортируйте результат в CSV или скопируйте его в буфер обмена.
            </li>
          </ol>
        </Section>

        <Section id="settings" title="3. Настройки">
          <h3>Провайдеры</h3>
          <p>
            Поддерживаются три провайдера. Каждый имеет свой формат идентификаторов моделей —
            не путайте их при ручном вводе:
          </p>
          <ul>
            <li>
              <b>OpenRouter</b> — модели в нижнем регистре через дефис, например{" "}
              <code>meta-llama/llama-3.3-70b-instruct</code>. Получить ключ:{" "}
              <a href="https://openrouter.ai/keys" target="_blank" rel="noreferrer">openrouter.ai/keys</a>.
            </li>
            <li>
              <b>GitHub Models</b> — модели в смешанном регистре, например{" "}
              <code>meta/Llama-3.3-70B-Instruct</code> или <code>openai/gpt-4o-mini</code>. Нужен <i>fine-grained PAT</i>
              {" "}c правом <i>Models (read)</i> или классический PAT. Авторизуйте PAT в настройках организации,
              если у вас включён SSO. Заголовок авторизации — <code>Token</code>, а не <code>Bearer</code>.
            </li>
            <li>
              <b>Google Gemini</b> — модели вида <code>gemini-2.5-flash</code>, <code>gemini-2.0-flash</code>.
              Получить ключ: <a href="https://aistudio.google.com/apikey" target="_blank" rel="noreferrer">aistudio.google.com/apikey</a>.
            </li>
          </ul>
          <p>
            Кнопка <b>Проверить подключение</b> делает короткий запрос с моделью по умолчанию для этого
            провайдера (см. вкладку <b>По умолчанию</b>). Если ключ не задан — кнопка неактивна.
            Поле <i>Base URL (optional)</i> можно оставить пустым: будет использован стандартный endpoint.
          </p>

          <h3>Модели</h3>
          <p>
            На этой вкладке отображается список <i>предзаданных</i> моделей для каждого провайдера и
            возможность добавить <i>свои</i>. Добавленные модели появятся в подсказках на форме новой задачи и в выпадающих списках.
          </p>

          <h3>Промпты</h3>
          <p>
            Здесь редактируются шаблоны системных промптов, отправляемых модели. <b>Промпты сохранены
            на английском языке намеренно</b> — модели лучше следуют инструкциям именно на английском,
            особенно в части строгого подсчёта и формата JSON.
          </p>
          <p>
            В промптах используются плейсхолдеры в двойных скобках, например{" "}
            <code>{"{{ENTRIES_BLOCK}}"}</code>, <code>{"{{RATIO_BLOCK}}"}</code>,{" "}
            <code>{"{{INPUT_COUNT}}"}</code>. Они подставляются автоматически при отправке. Кнопка{" "}
            <b>Сбросить</b> вернёт промпт к значению по умолчанию.
          </p>
          <p>
            Перед запуском генерации можно нажать <b>Предпросмотр промпта</b> на форме задачи и увидеть
            ровно тот текст, который будет отправлен в API.
          </p>

          <h3>По умолчанию</h3>
          <p>
            Здесь задаются:
          </p>
          <ul>
            <li>
              <b>Провайдер по умолчанию</b> — какой провайдер выбран на форме новой задачи изначально.
            </li>
            <li>
              <b>Модель по умолчанию для каждого провайдера</b> — используется для проверки подключения и
              как автозаполнение поля «Модель» при смене провайдера на форме задачи.
            </li>
          </ul>
        </Section>

        <Section id="new-job" title="4. Создание задачи">
          <p>
            Форма новой задачи разделена на блоки: основные параметры, соотношение, распределение анкоров, бренды (только в режиме нескольких сайтов), CSV и AI-провайдер.
          </p>
          <ul>
            <li>
              <b>Название задачи</b> — произвольное. По умолчанию формируется как «План анкоров — &lt;дата&gt;».
            </li>
            <li>
              <b>Режим</b> — <i>Один сайт</i> или <i>Несколько сайтов</i> (см. раздел 5).
            </li>
            <li>
              <b>Всего анкоров</b> — поле только для чтения. Равно числу строк в CSV (один анкор на каждый <code>Target URL</code>).
            </li>
          </ul>
        </Section>

        <Section id="modes" title="5. Режимы: один сайт vs несколько">
          <h3>Один сайт</h3>
          <p>
            Все <code>Target URL</code> относятся к одному сайту. Распределение и соотношение применяются ко всему набору в целом.
          </p>

          <h3>Несколько сайтов</h3>
          <p>
            URL могут принадлежать разным брендам. AI определяет принадлежность к бренду по доменам, которые
            вы указали в блоке <b>Бренды</b>:
          </p>
          <ul>
            <li>На каждый бренд — одна строка: имя и домены (через запятую).</li>
            <li>
              Кнопка <b>Массовая вставка</b> позволяет добавить несколько брендов одной операцией. Формат —
              по одной строке на бренд: <code>Имя | домен1, домен2</code>, либо разделитель — табуляция (удобно
              вставлять прямо из Google Sheets). Можно добавить к существующим брендам или заменить весь список.
            </li>
            <li>
              В режиме нескольких сайтов <b>распределение анкоров и соотношение dofollow применяются отдельно
              к каждому бренду</b>, а не только к общему набору.
            </li>
          </ul>
        </Section>

        <Section id="distribution" title="6. Распределение и категории анкоров">
          <p>
            Сумма процентов по 4 категориям должна равняться 100%. Если сумма не равна — кнопка генерации сообщит об ошибке.
          </p>
          <ul>
            <li>
              <b>Общие (generic)</b> — описательные «click here», «эта страница», «подробнее», «читать далее» и т.д.
            </li>
            <li>
              <b>Брендовые (branded)</b> — содержат имя бренда (или его домен), привязанного к целевому URL.
            </li>
            <li>
              <b>Ключевые (keyword)</b> — содержат ключевые слова, описывающие тематику страницы.
            </li>
            <li>
              <b>URL</b> — текст анкора <i>равен самому Target URL</i> (полный URL, символ в символ). Парсер
              принудительно подставляет URL в текст анкора, даже если AI вернул что-то другое — это гарантия консистентности.
            </li>
          </ul>
          <p>
            Вы можете посмотреть, какие именно категории сгенерировал AI, в столбце <b>Категория</b> на странице задачи. Цвет столбца отражает категорию.
          </p>
        </Section>

        <Section id="ratios" title="7. Соотношение dofollow / nofollow">
          <p>
            Если переключатель <b>Соотношение dofollow / nofollow</b> включён, в промпте указывается
            требуемое целое число <code>dofollow</code> и <code>nofollow</code> анкоров. Если выключен —
            статус ссылок не указывается вовсе и колонка <b>Follow</b> в таблице исчезает.
          </p>
          <p>
            Реальный результат может слегка отклоняться от целевого — модели иногда сбиваются на 1–2 анкора.
            Для точной подгонки <i>без обращения к AI</i> используйте кнопку <b>Подогнать соотношение</b>{" "}
            (раздел 14).
          </p>
        </Section>

        <Section id="csv" title="8. Загрузка CSV">
          <p>
            Формат CSV:
          </p>
          <ul>
            <li>
              Обязательная колонка: <code>Target URL</code> (регистр не важен — <code>target url</code> и{" "}
              <code>TARGET URL</code> также подходят).
            </li>
            <li>
              Опциональные: <code>Title</code>, <code>Keywords</code>. Если их нет — модель работает только с URL.
            </li>
            <li>
              В одной ячейке <code>Keywords</code> можно указать несколько ключевых слов. Они считаются как одна запись.
            </li>
          </ul>
          <p>
            Можно <b>загрузить файл</b> или вставить содержимое в текстовое поле. Под полем отображается
            счётчик распознанных записей. Если в части строк отсутствует <code>Target URL</code> — будет
            показано предупреждение «Пропущено N строк с пустым Target URL».
          </p>
          <p>
            Если CSV содержит только одну колонку — это нормально, инструмент корректно распознает его.
          </p>
        </Section>

        <Section id="running" title="9. Запуск, батчи и прогресс">
          <p>
            После нажатия <b>Создать и сгенерировать</b>:
          </p>
          <ol>
            <li>Задача сохраняется в БД, ей присваивается статус <code>running</code>.</li>
            <li>Входные данные разбиваются на батчи по 10 URL.</li>
            <li>
              Браузер последовательно выполняет батчи: каждый — отдельный запрос к AI. Между батчами{" "}
              пауза ~1.5 секунды, чтобы не упереться в лимиты.
            </li>
            <li>
              При получении <code>429 / rate limit</code> — клиент делает экспоненциальную задержку
              (3с → 6с → 12с → 30с) и повторяет тот же батч.
            </li>
            <li>
              На странице задачи отображается прогресс-бар: <i>Батч X / Y · Z из N анкоров</i>.
            </li>
          </ol>
          <p>
            Вы можете в любой момент:
          </p>
          <ul>
            <li>
              Нажать <b>Пауза</b> — текущая работа остановится, уже сгенерированные анкоры сохраняются.
            </li>
            <li>
              Нажать <b>Продолжить</b> — задача возобновится с того батча, на котором остановилась.
            </li>
          </ul>
          <p>
            Если в каком-то батче случилась ошибка (не rate-limit) — задача переходит в статус{" "}
            <i>partial</i> или <i>failed</i>, и текст ошибки показывается на странице. Можно нажать{" "}
            <b>Возобновить</b>, чтобы продолжить с этого же батча.
          </p>
          <p className="text-[var(--color-text-dim)] text-sm border-l-2 border-[var(--color-warn)] pl-3">
            <b>Важно:</b> генерация выполняется в браузере. Если вы закроете вкладку — оркестрация
            остановится. Открыв задачу заново, нажмите <b>Возобновить</b>, чтобы продолжить.
          </p>
        </Section>

        <Section id="results" title="10. Работа с результатами">
          <p>
            После завершения генерации (или во время неё) в карточке <b>Анкоры</b> отображается таблица:
          </p>
          <ul>
            <li><b>Target URL</b> — на какой URL ставится ссылка.</li>
            <li><b>Follow</b> (если включено соотношение) — <code>dofollow</code> или <code>nofollow</code>. Можно изменить инлайн.</li>
            <li><b>Категория</b> — цветной значок, к какой из 4 категорий относится анкор.</li>
            <li><b>Текст анкора</b> — кликните по тексту, чтобы открыть редактор и вручную поправить.</li>
          </ul>
          <p>
            Любая ручная правка <b>помечается как «manually edited»</b>. При выравнивании (раздел 13)
            такие анкоры всегда сохраняются — их не трогает ни <i>Surgical fix</i>, ни <i>Replace all AI</i>.
          </p>
          <p>
            <b>Чекбоксы слева от строк</b> позволяют выбрать произвольный набор анкоров для:
          </p>
          <ul>
            <li>
              <b>Перегенерации</b> — кнопка «Перегенерировать (N)». AI сгенерирует новые анкоры
              взамен выбранных, сохраняя их распределение анкоров по категориям.
            </li>
          </ul>
        </Section>

        <Section id="filters" title="11. Фильтры и пагинация">
          <p>
            В строке фильтров над таблицей доступны три инструмента:
          </p>
          <ul>
            <li>
              <b>Категория</b> — нажимайте на цветные плашки <i>generic / branded / keyword / url</i>, чтобы
              включать или выключать категории в выдаче.
            </li>
            <li>
              <b>Сайт</b> (только в режиме нескольких сайтов) — выпадающий список с поиском и чекбоксами.
              Пустой выбор = «все сайты»; выбрав один или несколько сайтов, вы оставляете только их анкоры.
            </li>
            <li>
              <b>Фильтр по тексту</b> — поиск по подстроке в тексте анкора, без учёта регистра.
              Например, <code>example</code> покажет все анкоры, где встречается это слово.
            </li>
          </ul>
          <p>
            <b>Пагинация:</b> по умолчанию показывается 50 анкоров на страницу. Доступны 50 / 100 / 200 / 500 / 1000.
            Внизу таблицы — навигация «Назад / Далее» и счётчик «Показано X-Y из N».
          </p>
          <p>
            Чекбокс <b>«выбрать все»</b> в шапке таблицы выбирает все анкоры, попавшие под текущие
            фильтры (даже на других страницах) — удобно для массовой перегенерации, например, всех «branded» по конкретному бренду.
          </p>
        </Section>

        <Section id="compare" title="12. Цель vs факт">
          <p>
            Карточка <b>Цель vs факт</b> показывает, насколько фактическое распределение анкоров совпадает с заданным.
          </p>
          <ul>
            <li>
              В режиме <b>одного сайта</b> — 5 плиток: <code>dofollow</code>, <code>generic</code>,{" "}
              <code>branded</code>, <code>keyword</code>, <code>url</code>. Зелёный значит «попадание»,
              красный — «отклонение от цели».
            </li>
            <li>
              В режиме <b>нескольких сайтов</b> — таблица: строка на каждый бренд + строка <i>Итого</i>.
              Каждая ячейка цветом показывает, насколько отклонение существенно.
            </li>
          </ul>
          <p>
            В режиме нескольких сайтов рядом со строками брендов есть чекбоксы — отметив их,
            можно запустить выравнивание.
          </p>
        </Section>

        <Section id="rebalance" title="13. Выравнивание распределения анкоров">
          <p>
            Если фактическое распределение анкоров по бренду отклоняется от целевого, не нужно перегенерировать
            всю задачу. Достаточно:
          </p>
          <ol>
            <li>В таблице <b>Цель vs факт</b> отметить чекбоксы у тех брендов, где есть расхождение.</li>
            <li>Нажать <b>Выровнять (N)</b>.</li>
            <li>Выбрать режим в открывшемся диалоге:</li>
          </ol>
          <ul>
            <li>
              <b>Точечное выравнивание (surgical fix)</b> — оставить анкоры AI, которые уже соответствуют цели.
              Удалить только избыточные категории и сгенерировать недостающие. Оптимально по токенам.
            </li>
            <li>
              <b>Заменить все анкоры AI</b> — удалить все сгенерированные AI анкоры этого бренда и создать
              новый набор с нуля. Более предсказуемый результат, но дороже по токенам.
            </li>
          </ul>
          <p>
            <b>Анкоры, отредактированные вручную, всегда сохраняются.</b> Они учитываются при подсчёте новых
            целей: AI получит точные количества по категориям и follow-status, скорректированные с учётом
            ручных правок.
          </p>
          <p>
            Бренды обрабатываются по очереди. Между ними — пауза ~1.2 секунды. Сверху отображается
            прогресс-бар с текущим брендом, прошедшим временем и оценкой оставшегося времени. Кнопка{" "}
            <b>Остановить после текущего</b> мягко прерывает процесс — текущий бренд завершит обработку,
            а остальные пропустятся.
          </p>
          <p className="text-[var(--color-text-dim)] text-sm border-l-2 border-[var(--color-warn)] pl-3">
            <b>Важно:</b> в режиме «Заменить все анкоры AI» происходит сначала удаление, потом запрос к AI.
            Если AI вернёт ошибку — у бренда останутся только ручные анкоры. Перезапустите выравнивание
            повторно или используйте «Точечное выравнивание».
          </p>
        </Section>

        <Section id="quickfix" title="14. Подгонка соотношения dofollow">
          <p>
            Кнопка <b>Подогнать соотношение</b> на странице задачи флипает минимально необходимое число анкоров
            <code> dofollow ↔ nofollow</code>, чтобы точно попасть в целевой процент. <b>Без обращения к AI</b>{" "}
            и без расхода токенов.
          </p>
          <ul>
            <li>В режиме <b>одного сайта</b> подгоняет соотношение по всему набору.</li>
            <li>В режиме <b>нескольких сайтов</b> — отдельно по каждому бренду (а не суммарно по всей задаче).</li>
            <li>
              Сообщение по итогу: «Перевёрнуто N анкоров на M сайтов для попадания в X% dofollow по каждому сайту».
              Если все бренды уже точны — будет сообщение «Все N сайтов уже соответствуют X%».
            </li>
          </ul>
          <p>
            Округление: если у бренда 7 анкоров и цель 70%, целое значение — 5 анкоров (5/7 = 71.4%).
            Это нормальное поведение, на малых выборках идеально не попасть в процент.
          </p>
        </Section>

        <Section id="edit-rerun" title="15. Редактирование и перезапуск задачи">
          <h3>Редактирование задачи</h3>
          <p>
            Кнопка <b>Редактировать задачу</b> на странице задачи открывает форму, идентичную форме
            создания, но с уже заполненными значениями. Можно поменять любой параметр: критерии,
            распределение анкоров, список брендов, входные данные.
          </p>
          <ul>
            <li>
              <b>Только сохранить</b> — обновляет задачу, существующие анкоры остаются. Удобно, если поменяли
              имя или ошиблись с криетерием, но не хотите тратить токены.
            </li>
            <li>
              <b>Сохранить и перезапустить</b> — обновляет задачу и запускает свежую генерацию (старые анкоры заменяются).
            </li>
          </ul>

          <h3>Переименование</h3>
          <p>
            Кликните по заголовку задачи на её странице — он превратится в поле ввода. Также можно
            переименовать прямо в списке задач (значок карандаша рядом со строкой).
          </p>

          <h3>Перезапуск</h3>
          <p>
            Кнопка <b>Перезапустить всё</b> на странице задачи или кнопка <b>Перезапустить</b> в списке —
            заново запускают генерацию с теми же параметрами.
          </p>

          <h3>Удаление</h3>
          <p>
            Кнопки <b>Удалить</b> на странице задачи и в списке. Подтверждение — обязательно. Удаление каскадное:
            вместе с задачей удаляются её входные данные и анкоры.
          </p>
        </Section>

        <Section id="export" title="16. Экспорт результатов">
          <p>
            Кнопки в шапке карточки <b>Анкоры</b>:
          </p>
          <ul>
            <li>
              <b>Экспорт CSV</b> — скачивает CSV файл с колонками <code>Target URL</code>, <code>Follow</code>{" "}
              (если включено), <code>Category</code>, <code>Anchor</code>. Имя файла — название задачи.
            </li>
            <li>
              <b>Копировать</b> — копирует таблицу в буфер обмена в формате с табуляцией (можно вставить в Google Sheets / Excel).
            </li>
          </ul>
          <p>
            Экспорт не учитывает фильтры — выгружается полный набор анкоров задачи. Если нужно выгрузить
            только подмножество — отфильтруйте, выделите нужные строки чекбоксами и используйте копирование.
          </p>
        </Section>

        <Section id="troubleshooting" title="17. Решение проблем">
          <h3>«401 Unauthorized» при проверке подключения</h3>
          <ul>
            <li>Убедитесь, что API-ключ скопирован полностью, без лишних пробелов.</li>
            <li>
              Для GitHub Models: PAT должен иметь право <code>Models (read)</code>. Если у организации
              включён SSO — авторизуйте PAT в настройках GitHub.
            </li>
            <li>
              Если API-ключ не сохраняется или 401 повторяется — нажмите <b>Сохранить настройки</b> до
              нажатия <b>Проверить подключение</b>. Тест использует значения из БД.
            </li>
          </ul>

          <h3>«400 Bad Request: model is not valid»</h3>
          <p>
            Скорее всего ID модели не подходит провайдеру. У OpenRouter и GitHub Models разный формат
            идентификаторов (см. раздел 3). Проверьте, что в поле <i>Модель по умолчанию</i> для каждого
            провайдера указан именно его формат.
          </p>

          <h3>Задача «зависла» на 1000 анкорах</h3>
          <p>
            В реальности её нельзя сгенерировать одним запросом — токены не помещаются в ответ. Инструмент
            автоматически разбивает задачу на батчи по 10 URL. Если страница задачи долго не показывает прогресс — проверьте, что:
          </p>
          <ul>
            <li>Вкладка не была закрыта (оркестрация выполняется в браузере).</li>
            <li>Не упёрлись в rate limit (увидите соответствующее сообщение в шапке).</li>
            <li>API-ключ корректен.</li>
          </ul>
          <p>
            Если задача в статусе <i>failed</i> или <i>partial</i> — нажмите <b>Возобновить</b>: оркестрация продолжится с того
            батча, где остановилась. Уже сгенерированные анкоры сохранены.
          </p>

          <h3>«Auto-detect delimiter» при загрузке CSV</h3>
          <p>
            Это предупреждение PapaParse, оно подавлено в инструменте. Если у вас CSV с одной колонкой
            (только Target URL) — данные распознаются корректно.
          </p>

          <h3>Распределение сильно отклоняется от цели</h3>
          <p>
            На малых количествах URL (меньше 30 на бренд) даже хорошие модели могут промахиваться на 5–10%.
            Запустите <b>Выравнивание</b> (раздел 13) — это решит вопрос точечно и без полной перегенерации.
          </p>

          <h3>Анкоры с категорией «url» содержат не URL</h3>
          <p>
            Парсер принудительно подставляет <code>Target URL</code> в текст анкора при категории <code>url</code>.
            Если вы видите что-то другое — значит анкор был отредактирован вручную (пометка <i>manually edited</i>).
            Удалите его или используйте режим <b>Заменить все анкоры AI</b> при выравнивании, чтобы пересоздать.
          </p>

          <h3>Где хранятся данные</h3>
          <p>
            Локально, в SQLite-файле проекта (<code>data/app.db</code>). API-ключи также хранятся локально
            в файле настроек — никуда не отправляются, кроме самого API провайдера при запросах.
          </p>
        </Section>

        <footer className="border-t border-[var(--color-border)] pt-6 mt-10 text-sm text-[var(--color-text-dim)]">
          <p>
            Если в документации чего-то не хватает или нашли ошибку — отредактируйте файл{" "}
            <code>src/app/docs/page.tsx</code> и пересоберите проект.
          </p>
        </footer>
      </article>
    </div>
  );
}

function Section({ id, title, children }: { id: string; title: string; children: React.ReactNode }) {
  return (
    <section id={id} className="scroll-mt-20">
      <h2 className="text-2xl font-semibold mb-4 border-b border-[var(--color-border)] pb-2">
        <a href={`#${id}`} className="hover:text-[var(--color-accent)]">
          {title}
        </a>
      </h2>
      <div className="space-y-3 leading-relaxed text-[var(--color-text)]">
        {children}
      </div>
    </section>
  );
}

function Doclink({ href, children }: { href: string; children: React.ReactNode }) {
  return (
    <Link href={href} className="text-[var(--color-accent)] hover:underline">
      {children}
    </Link>
  );
}
